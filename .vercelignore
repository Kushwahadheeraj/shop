# Ignore ONLY root-level files to prevent Vercel from installing at root level
# This ensures all dependencies are installed only in app/dashboard
# Use leading slash to match only root-level files
/package.json
/package-lock.json
/node_modules
/backend
/app/shop

