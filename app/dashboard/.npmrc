# Force npm to treat this directory as a standalone project
# Prevents hoisting dependencies (like next) outside app/dashboard
# This ensures Vercel bundles all required Next.js files correctly
workspaces=false
